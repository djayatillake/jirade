#!/bin/bash
# Build the jira-agent Docker image
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

docker build -t jira-agent:latest -f "$SCRIPT_DIR/Dockerfile" "$SCRIPT_DIR"

echo "Built jira-agent:latest"
